"npm start" to run.

You need to go to "firebase.google.com" and add a project. Then, go into "Add Firebase to your web app" and copy the content into the firebase.js file we have in the "src" folder. Finally, go to "Authentication" on the left side menu on "firebase.google.com", go into "Sign-in Method" and enable "Email/Password".
